# OpenAI (если задан — используется нативный API api.openai.com, PROXY_API_KEY не нужен)
# OPENAI_KEY=sk-...

# ProxyAPI Configuration (если OPENAI_KEY не задан)
# Получить ключ: https://proxyapi.ru/
# Документация: https://proxyapi.ru/docs/openai-text-generation

PROXY_API_KEY=your_proxy_api_key_here

# Модели (опционально, по умолчанию gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini

# Доступные модели ProxyAPI:
# - gpt-4o (лучшее качество)
# - gpt-4o-mini (баланс цена/качество)
# - gpt-4-turbo
# - gpt-3.5-turbo

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Парсер сайтов (Playwright Chromium + playwright-stealth)
# После pip install -r requirements.txt выполните: playwright install chromium
# PARSER_TIMEOUT=30 — таймаут для body/локаторов (сек)
# PARSER_NAVIGATION_TIMEOUT=60 — только page.goto (сек); первый холодный старт Chromium + медленный ответ сайта
# PARSER_HEADLESS=false — реже палится headless-детект (нужен дисплей / сервер с GUI)
# PARSER_USER_AGENT=...  — полный UA как у Chrome
# PARSER_LOCALE=en-US
# PARSER_TIMEZONE=Europe/Moscow
# PARSER_ACCEPT_LANGUAGE=en-US,en;q=0.9
# PARSER_NAVIGATOR_LANGUAGES=en-US,en
# PARSER_PLATFORM=Win32

